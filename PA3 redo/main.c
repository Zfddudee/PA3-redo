#include "Header.h"

int main(void)
{
	//FILE* input = fopen("musicPlayList.csv", "r");
	//if (input == NULL) perror("Error");

	Node* pHead = NULL; // empty list
	mainMenu(&pHead);
	//load(&pHead);

	/*Record C1 = { "Deane", "4255551552", "zach@wsu.edu", "zach" };
	Record C2 = { "Dane", "9999999999", "zach@wsu.edu", "RH" };
	Record C3 = { "Ane", "9999999999", "zach@wsu.edu", "GHAAAA" };

	insertMusicInOrder(&pHead, C1);
	insertMusicInOrder(&pHead, C2);
	insertMusicInOrder(&pHead, C3);
	print(pHead);

	deleteNode(&pHead, "zach");
	print(pHead);

	editNode(&pHead);
	print(pHead);*/




	//fclose(input);
}